# Radar Económico — Claude Instructions

## After every code change

After making any code change, always:

1. Stage the modified files (`git add <files>`)
2. Commit with a concise message following the existing style (`feat:`, `fix:`, `style:`, etc.)
3. Push to origin (`git push`)

Vercel deploys automatically on push, so this gets every change live immediately.
